security:
  jwt:
    public-key: classpath:app.pub
    private-key: classpath:app.key
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DATABASE_URL:locahost}:${DATABASE_PORT:3306}/${DATABASE_NAME:fittogether}
    username: ${DATABASE_USER:root}
    password: ${DATABASE_PASSWORD:123$qweR}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    url: jdbc:mysql://${DATABASE_URL:locahost}:${DATABASE_PORT:3306}/fittogether
    user: ${DATABASE_USER:root}
    password: ${DATABASE_PASSWORD:123$qweR}
  mail:
    host: smtp.gmail.com
    port: 587
    username: dev.paranhos@gmail.com
    password: kdzi wbmv gkjj ouja
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com